# Registration-Portal
Ready to use Registration Portal in which User can register his self.
User can check his registered info by "Search by CNIC number" feature.
It consists on Firebase(database) & Cloudinary(storage).
Anyone can use this Ready made Registration Form by just pasting API of Firebase & Cloudinary.
